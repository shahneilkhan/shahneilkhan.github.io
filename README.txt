SNK PORTFOLIO UPDATE - README
================================

This zip has 2 things:

1) admin/index.html
   -> Upload this directly to your GitHub repo at path: admin/index.html
   -> This REPLACES your old admin page entirely (ready to use as-is).

2) hero-snippet-TO-PASTE-INTO-HOMEPAGE.html
   -> This is NOT a full page. Open your homepage's index.html on GitHub,
      find the old <section class="hero">...</section> part, delete it,
      and paste in the content of this file in its place.
   -> (Claude doesn't have your full current homepage file, so this
      couldn't be merged automatically - it has to be pasted in manually.)

After both are done:
- Go to admin/index.html on your live site, log in with your Firebase
  email/password, edit the Hero text, and click Save.
- Refresh your homepage - the new text should appear.
